import 'package:flutter/material.dart';

class RecommendeItem extends StatelessWidget {
  const RecommendeItem({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('hi');
  }
}
