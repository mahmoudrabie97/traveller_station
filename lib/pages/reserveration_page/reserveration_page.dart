import 'package:flutter/material.dart';

class ReserverationPage extends StatelessWidget {
  const ReserverationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Favourite Page'),
      ),
      body: Column(
        children: [Text('Favourite Page ')],
      ),
    );
  }
}
